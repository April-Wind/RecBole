# RecBole 知识图谱三元组文件构建指南

## 📋 目录

1. [简介](#简介)
2. [文件格式说明](#文件格式说明)
3. [快速开始](#快速开始)
4. [详细教程](#详细教程)
5. [示例说明](#示例说明)
6. [常见问题](#常见问题)

---

## 简介

本指南介绍如何使用RecBole推荐系统库中的方法构建知识图谱三元组文件。知识图谱可以为推荐系统提供丰富的实体关系信息，提升推荐效果。

RecBole支持基于知识图谱的推荐模型，如：
- **CKE** (Collaborative Knowledge base Embedding)
- **KGAT** (Knowledge Graph Attention Network)
- **KGCN** (Knowledge Graph Convolutional Network)
- **RippleNet**
- **KGIN** (Knowledge Graph Integrated Network)
- 等等...

---

## 文件格式说明

RecBole的知识图谱数据包含两个核心文件：

### 1. 知识图谱三元组文件 (`.kg`)

**格式要求：**
```
head_id:token	relation_id:token	tail_id:token
实体1	关系类型	实体2
实体1	关系类型	实体3
...
```

**说明：**
- 第一行必须是字段定义（包含 `:token` 类型标注）
- 使用**制表符** (`\t`) 分隔字段
- `head_id`: 头实体的ID
- `relation_id`: 关系类型
- `tail_id`: 尾实体的ID

**示例：**
```
head_id:token	relation_id:token	tail_id:token
movie_1	directed_by	director_nolan
movie_1	has_actor	actor_dicaprio
movie_1	has_genre	genre_scifi
movie_2	directed_by	director_tarantino
```

### 2. 物品-实体链接文件 (`.link`)

**格式要求：**
```
item_id:token	entity_id:token
物品ID1	实体ID1
物品ID2	实体ID2
...
```

**说明：**
- 第一行必须是字段定义（包含 `:token` 类型标注）
- 使用**制表符** (`\t`) 分隔字段
- `item_id`: 推荐系统中的物品ID（与 `.inter` 文件中的item_id对应）
- `entity_id`: 知识图谱中的实体ID（与 `.kg` 文件中的实体对应）

**示例：**
```
item_id:token	entity_id:token
1	movie_1
2	movie_2
3	movie_3
```

---

## 快速开始

### 步骤1: 运行示例脚本

我们提供了一个完整的示例脚本 `build_kg_example.py`，可以快速生成知识图谱文件：

```bash
# 运行所有示例（推荐首次使用）
python build_kg_example.py

# 仅运行电影推荐示例
python build_kg_example.py --example movie

# 仅运行商品推荐示例
python build_kg_example.py --example product

# 仅查看格式指南
python build_kg_example.py --example guide
```

### 步骤2: 查看生成的文件

运行后会在 `./dataset/` 目录下生成示例文件：

```
dataset/
├── movie_example/
│   ├── movie_example.kg      # 电影知识图谱三元组
│   └── movie_example.link    # 电影-实体链接
└── product_example/
    ├── product_example.kg    # 商品知识图谱三元组
    └── product_example.link  # 商品-实体链接
```

### 步骤3: 在RecBole中使用

创建配置文件 `config.yaml`:

```yaml
# 数据集配置
USER_ID_FIELD: user_id
ITEM_ID_FIELD: item_id
HEAD_ENTITY_ID_FIELD: head_id
TAIL_ENTITY_ID_FIELD: tail_id
RELATION_ID_FIELD: relation_id
ENTITY_ID_FIELD: entity_id

# 加载数据列
load_col:
    inter: [user_id, item_id]       # 交互数据
    kg: [head_id, relation_id, tail_id]  # 知识图谱三元组
    link: [item_id, entity_id]      # 物品-实体链接

# 模型配置
embedding_size: 64
kg_embedding_size: 64
```

然后运行模型：

```bash
python run_recbole.py --model=CKE --dataset=movie_example --config_files=config.yaml
```

---

## 详细教程

### 方法1: 使用三元组列表构建

适合数据量较小或需要精确控制每个三元组的场景。

```python
from build_kg_example import KnowledgeGraphBuilder

# 创建构建器
builder = KnowledgeGraphBuilder(
    dataset_name='my_dataset',
    output_dir='./dataset'
)

# 定义知识图谱三元组
kg_triples = [
    ('movie_1', 'directed_by', 'director_nolan'),
    ('movie_1', 'has_actor', 'actor_dicaprio'),
    ('movie_2', 'has_genre', 'genre_action'),
]

# 定义物品-实体链接
item_links = [
    ('1', 'movie_1'),
    ('2', 'movie_2'),
]

# 生成文件
builder.build_kg_triples(kg_triples)
builder.build_item_entity_links(item_links)
```

### 方法2: 使用字典构建

适合有层级结构或需要批量处理的场景。

```python
from build_kg_example import KnowledgeGraphBuilder

# 创建构建器
builder = KnowledgeGraphBuilder(
    dataset_name='my_dataset',
    output_dir='./dataset'
)

# 定义知识图谱数据（字典格式）
kg_data = {
    'movie_1': [
        ('directed_by', 'director_nolan'),
        ('has_actor', 'actor_dicaprio'),
        ('has_genre', 'genre_scifi'),
    ],
    'movie_2': [
        ('has_genre', 'genre_action'),
    ],
}

# 定义物品-实体映射
item_entity_map = {
    '1': 'movie_1',
    '2': 'movie_2',
}

# 生成文件
builder.build_from_dict(kg_data, item_entity_map)
```

### 方法3: 从外部数据源构建

如果你有外部知识库（如DBpedia、Freebase等），可以这样处理：

```python
import pandas as pd
from build_kg_example import KnowledgeGraphBuilder

# 假设从外部API或数据库获取数据
# 示例：从CSV文件读取
external_kg = pd.read_csv('external_knowledge.csv')

# 转换为三元组格式
triples = []
for _, row in external_kg.iterrows():
    triples.append((row['subject'], row['predicate'], row['object']))

# 创建构建器并生成文件
builder = KnowledgeGraphBuilder('external_dataset', './dataset')
builder.build_kg_triples(triples)

# 建立物品到实体的映射
# 这里需要根据你的实际情况来映射
item_links = [
    (str(i), f"entity_{i}") for i in range(1, 101)
]
builder.build_item_entity_links(item_links)
```

---

## 示例说明

### 电影推荐示例

**场景描述：**
构建一个电影推荐系统的知识图谱，包含电影、导演、演员、类型等实体及其关系。

**知识图谱结构：**
```
电影 ---directed_by---> 导演
电影 ---has_actor---> 演员
电影 ---has_genre---> 类型
导演 ---won_award---> 奖项
演员 ---collaborated_with---> 演员
```

**生成的文件内容示例：**

`movie_example.kg`:
```
head_id:token	relation_id:token	tail_id:token
movie_1	directed_by	director_nolan
movie_1	has_actor	actor_dicaprio
movie_1	has_genre	genre_scifi
movie_2	directed_by	director_tarantino
movie_2	has_actor	actor_travolta
```

`movie_example.link`:
```
item_id:token	entity_id:token
1	movie_1
2	movie_2
3	movie_3
```

### 商品推荐示例

**场景描述：**
构建一个电商推荐系统的知识图谱，包含商品、品牌、类别、特性等实体及其关系。

**知识图谱结构：**
```
商品 ---brand---> 品牌
商品 ---category---> 类别
商品 ---has_feature---> 特性
品牌 ---headquartered_in---> 地点
```

**生成的文件内容示例：**

`product_example.kg`:
```
head_id:token	relation_id:token	tail_id:token
product_laptop_1	brand	brand_dell
product_laptop_1	category	category_electronics
product_laptop_1	has_feature	feature_16gb_ram
product_laptop_2	brand	brand_apple
```

`product_example.link`:
```
item_id:token	entity_id:token
101	product_laptop_1
102	product_laptop_2
103	product_phone_1
```

---

## 常见问题

### Q1: 为什么需要 `.link` 文件？

**A:** `.link` 文件建立了推荐系统中的物品ID（item_id）和知识图谱中的实体ID（entity_id）之间的映射关系。这是必需的，因为：
- 推荐系统的 `.inter` 文件中使用的是简单的item_id（如1, 2, 3...）
- 知识图谱中使用的是有意义的实体ID（如movie_1, product_laptop_1...）
- 需要通过 `.link` 文件将两者关联起来

### Q2: 知识图谱的数据从哪里获取？

**A:** 常见的知识图谱数据源包括：
- **公开知识库**: DBpedia, Freebase, Wikidata, YAGO等
- **领域知识库**: IMDB（电影）, MusicBrainz（音乐）等
- **自建知识库**: 根据业务数据构建
- **第三方API**: 如Google Knowledge Graph API

### Q3: 如何确定实体和关系？

**A:** 设计知识图谱时需要考虑：
1. **实体选择**: 选择对推荐任务有帮助的实体（如电影、导演、演员、类型）
2. **关系定义**: 定义有意义的关系（如directed_by, has_actor, has_genre）
3. **粒度控制**: 平衡知识图谱的规模和质量
4. **数据质量**: 确保三元组的准确性

### Q4: 如何处理大规模知识图谱？

**A:** 对于大规模知识图谱：
1. **分批处理**: 使用 pandas 的 `chunksize` 参数分批读取和写入
2. **过滤筛选**: 只保留与推荐任务相关的实体和关系
3. **实体对齐**: 使用实体对齐技术减少冗余
4. **并行处理**: 使用多进程处理大规模数据

示例代码：
```python
import pandas as pd

# 分批处理大文件
chunk_size = 10000
triples = []

for chunk in pd.read_csv('large_kg.csv', chunksize=chunk_size):
    for _, row in chunk.iterrows():
        # 进行必要的过滤和转换
        if is_relevant(row):
            triples.append((row['head'], row['relation'], row['tail']))

builder.build_kg_triples(triples)
```

### Q5: 生成的文件可以直接使用吗？

**A:** 生成 `.kg` 和 `.link` 文件后，还需要：
1. **准备交互文件** (`.inter`): 包含用户-物品交互记录
2. **准备配置文件**: 设置字段映射和模型参数
3. **验证数据**: 确保物品ID在 `.inter` 和 `.link` 文件中一致

完整的数据集应包含：
```
dataset/
└── my_dataset/
    ├── my_dataset.inter    # 用户-物品交互
    ├── my_dataset.kg       # 知识图谱三元组
    └── my_dataset.link     # 物品-实体链接
```

### Q6: 支持哪些知识感知推荐模型？

**A:** RecBole支持多种知识感知推荐模型：

| 模型 | 说明 |
|------|------|
| CKE | 基于知识库的协同嵌入 |
| KGAT | 知识图谱注意力网络 |
| KGCN | 知识图谱卷积网络 |
| KGNNLS | 知识图谱神经网络标签平滑 |
| RippleNet | 基于知识图谱的传播网络 |
| MKR | 多任务知识图谱推荐 |
| KTUP | 知识感知元路径 |
| KGIN | 知识图谱集成网络 |
| MCCLK | 元上下文对比学习 |
| CFKG | 协同过滤知识图谱 |

使用示例：
```bash
# 使用CKE模型
python run_recbole.py --model=CKE --dataset=my_dataset

# 使用KGAT模型
python run_recbole.py --model=KGAT --dataset=my_dataset

# 使用RippleNet模型
python run_recbole.py --model=RippleNet --dataset=my_dataset
```

### Q7: 如何验证知识图谱文件是否正确？

**A:** 可以通过以下方式验证：

```python
from recbole.config import Config
from recbole.data import create_dataset

# 加载配置
config = Config(model='CKE', dataset='my_dataset')

# 创建数据集
dataset = create_dataset(config)

# 查看知识图谱统计信息
print(f"实体数量: {dataset.entity_num}")
print(f"关系数量: {dataset.relation_num}")
print(f"三元组数量: {len(dataset.kg_feat)}")
print(f"链接的物品数量: {len(dataset.item2entity)}")

# 查看部分数据
print("\n知识图谱三元组示例:")
print(dataset.kg_feat.head(10))

print("\n物品-实体映射示例:")
for item_id, entity_id in list(dataset.item2entity.items())[:5]:
    print(f"Item {item_id} -> Entity {entity_id}")
```

---

## 进阶用法

### 添加实体属性

除了基本的三元组，还可以为实体添加属性：

```python
# 扩展的知识图谱，包含属性
extended_kg = [
    # 基本关系
    ('movie_1', 'directed_by', 'director_nolan'),
    ('movie_1', 'has_actor', 'actor_dicaprio'),
    
    # 属性信息
    ('movie_1', 'has_rating', 'rating_8.8'),
    ('movie_1', 'release_year', 'year_2010'),
    ('director_nolan', 'nationality', 'country_uk'),
    ('actor_dicaprio', 'birth_year', 'year_1974'),
]
```

### 使用反向关系增强图谱

```python
def add_reverse_relations(triples, reverse_suffix='_reverse'):
    """为每个三元组添加反向关系"""
    extended_triples = list(triples)
    for head, relation, tail in triples:
        # 添加反向关系
        reverse_relation = relation + reverse_suffix
        extended_triples.append((tail, reverse_relation, head))
    return extended_triples

# 使用
kg_triples = [
    ('movie_1', 'directed_by', 'director_nolan'),
    ('movie_1', 'has_actor', 'actor_dicaprio'),
]

extended_triples = add_reverse_relations(kg_triples)
# 结果会包含:
# ('director_nolan', 'directed_by_reverse', 'movie_1')
# ('actor_dicaprio', 'has_actor_reverse', 'movie_1')
```

### 从关系数据库构建知识图谱

```python
import sqlite3
from build_kg_example import KnowledgeGraphBuilder

# 连接数据库
conn = sqlite3.connect('movie_database.db')

# 提取电影-导演关系
query_director = """
SELECT m.movie_id, d.director_id 
FROM movies m 
JOIN directors d ON m.director_id = d.id
"""
df_director = pd.read_sql_query(query_director, conn)

# 提取电影-演员关系
query_actor = """
SELECT m.movie_id, a.actor_id 
FROM movies m 
JOIN movie_actors ma ON m.id = ma.movie_id
JOIN actors a ON ma.actor_id = a.id
"""
df_actor = pd.read_sql_query(query_actor, conn)

# 构建三元组
triples = []
for _, row in df_director.iterrows():
    triples.append((f"movie_{row['movie_id']}", 'directed_by', f"director_{row['director_id']}"))

for _, row in df_actor.iterrows():
    triples.append((f"movie_{row['movie_id']}", 'has_actor', f"actor_{row['actor_id']}"))

# 生成知识图谱文件
builder = KnowledgeGraphBuilder('movie_db', './dataset')
builder.build_kg_triples(triples)
```

---

## 参考资源

1. **RecBole官方文档**: https://recbole.io/docs/
2. **知识感知推荐快速入门**: https://recbole.io/docs/user_guide/usage/running_new_dataset.html
3. **数据格式说明**: https://recbole.io/docs/user_guide/data_intro.html
4. **GitHub仓库**: https://github.com/RUCAIBox/RecBole
5. **论文**: RecBole: Towards a Unified, Comprehensive and Efficient Framework for Recommendation Algorithms

---

## 贡献

如果你在使用过程中发现问题或有改进建议，欢迎：
- 提交Issue: https://github.com/RUCAIBox/RecBole/issues
- 提交Pull Request
- 参与讨论

---

## 许可证

本项目基于 MIT License 开源。

---

**最后更新**: 2024年12月
**维护者**: RecBole团队
